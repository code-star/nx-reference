import { FC } from "react";

const Balance: FC = () => {
    return (
        <header className="flex" style={{ border: "1px solid red" }}>
            <h1>Balance: 0.1000000 BTC (€5,212.80)</h1>
        </header>
    );
};

export default Balance;
