import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="services/Message Service" />

# Message Service

Store warnings and errors

Add an error:

```ts
this.messageService.log(`MyService: ${message}`, 'error');
```

Show errors:

```ts
<div *ngFor="let message of messageService.messages" style="color: red;">{{ message }}</div>
```